function compareTriplets(a, b) {
  let count_a = 0;
  let count_b = 0;

  for (let i = 0; i < 3; i++) {
    if (a[i] > b[i]) {
      count_a++;
    } else if (a[i] < b[i]) {
      count_b++;
    }
  }
  return [count_a, count_b];
}
console.log(compareTriplets([17, 28, 30], [99, 16, 8]));
